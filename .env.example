# Copy this file to .env and fill in the key to enable the vision-model
# escalation fallback (used only when the camera view is ambiguous).
#
# Without a key, escalation disables itself automatically and the agent
# asks you directly whenever it's unsure. The game is fully playable
# offline either way.
OPENROUTER_API_KEY=
